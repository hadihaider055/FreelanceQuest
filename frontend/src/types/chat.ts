export type Chat = {
    chat_id: string;
    member_id: string;
    recipient_name: string;
    recipient_member_id: string;
    id: string;
}

export type Message = {
    message: string;
    sender_id: string;
}
